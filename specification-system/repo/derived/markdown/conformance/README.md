# Generic identity conformance construction boundary

The authoritative JSON construction artifacts remain controlling. This
projection summarizes their current non-normative construction boundary.

The conformance model defines closed positive and negative vector envelopes,
deterministic expected outcomes, exact vector identity and uniqueness rules,
coverage tags, complete discovery, and exactly-once execution.

Vectors may test only behavior already established by the canonical JSON,
identity-family, own-identity, reference, direct aggregate, verification, and
construction-evidence boundaries. They do not create new semantics.

Structural vector failures precede semantic execution. Existing deterministic
construction diagnostics remain exact; other failures use a closed diagnostic
class with stable evidence.

Accepted conformance, governing-revision binding, manifest bootstrap, sealing,
acceptance, product-specific evidence, and cutover remain unavailable.
