#!/usr/bin/env python3
from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[2]
SRC = ROOT / "product" / "src"
sys.path.insert(0, str(SRC))

from gve.cli import main

raise SystemExit(main())
